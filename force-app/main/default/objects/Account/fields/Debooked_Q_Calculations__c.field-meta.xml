<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Debooked_Q_Calculations__c</fullName>
    <description>MIS-8037</description>
    <externalId>false</externalId>
    <formula>(
  YEAR(DATEVALUE(Collection_Status_Debooked_Timestamp__c)) * 4
  + CEILING( MONTH(DATEVALUE(Collection_Status_Debooked_Timestamp__c)) / 3 )
)
&lt;=
(
  (YEAR(TODAY()) * 4 + CEILING(MONTH(TODAY()) / 3)) - 2
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Debooked Q Calculations</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
