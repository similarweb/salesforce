<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>No_secondary_solution_without_primary</fullName>
    <active>true</active>
    <description>Secondary solution can be populated only if Primary solution is populated</description>
    <errorConditionFormula>AND(
    ISBLANK(TEXT(Primary_Use_Case__c)),
    NOT(ISBLANK(Secondary_Use_Case__c))
    )</errorConditionFormula>
    <errorDisplayField>Primary_Solution__c</errorDisplayField>
    <errorMessage>Secondary Solution can be populated only if Primary Solution is populated</errorMessage>
</ValidationRule>
